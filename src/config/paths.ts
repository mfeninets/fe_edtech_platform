/**
 * Path aliases configuration
 * This file helps maintain consistency in path imports
 */

export const paths = {
  app: '@/app',
  components: '@/components',
  config: '@/config',
  features: '@/features',
  hooks: '@/hooks',
  lib: '@/lib',
  styles: '@/styles',
  testing: '@/testing',
  types: '@/types',
  utils: '@/utils',
} as const;
